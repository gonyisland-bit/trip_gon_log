# 프로젝트 작업 지침 (Project Guidelines)

## 1. 언어 원칙
- 모든 설명, 보고, 계획서, 커뮤니케이션은 **한국어**로만 진행합니다.

## 2. 작업 및 업무 프로세스 (원스톱 / 원큐 프로세스)
1. **계획 수립 (Planning)**:
   - 사용자의 새로운 기능 추가, 수정, 리팩토링 등 명령 지시 수신 시 먼저 한국어로 상세 **구현 계획서(Implementation Plan)**를 작성하여 제시합니다.
2. **사용자 승인 대기 (Approval)**:
   - 사용자가 검토 후 승인(Proceed)할 때까지 대기하며, 코드 수정을 임의로 먼저 진행하지 않습니다.
3. **구현 및 검증 (Execution & Verification)**:
   - 승인 즉시 작업을 수행하고 테스트/빌드 등 검증을 거칩니다.
4. **Git 반영 (Commit & Push)**:
   - 작업 완료 후 변경 사항을 git add, commit, push까지 한 번에 완료합니다.

## 3. 디자인 원칙 (Swiss Minimal Design Guidelines)
- **이모지 지양**: 촌스럽고 유치해 보일 수 있는 비규격 이모지(✈️, 📌, 💼, 🗓️ 등)는 UI 라벨이나 버튼에서 사용하지 않고, 정갈한 텍스트 또는 통일된 Lucide 아이콘을 사용합니다.
- **버튼 기호 중복 배제**: 버튼 사용 시 미니멀 디자인 아이콘과 동일한 의미의 기호나 텍스트를 중복 표기하지 않습니다 (예: `+` 아이콘 옆에 `+ ADD` 또는 `+ 추가`와 같이 기호 중복 금지 ➡️ `<Plus /> ADD` 또는 `<Plus /> 추가`로 단일화).
- **군더더기 설명 배제**: 사용자가 직관적으로 알 수 있는 자명한 기능(예: '일반 날짜', '날짜 클릭 시 이동', '선택 상태' 등)에 대한 부차적인 한글 설명 텍스트를 제거하여 여백과 집중도를 극대화합니다.
- **스위스 국제 타이포그래픽 스타일 (International Typographic Style)**:
  - 명확한 계층 구조의 산세리프(Sans) / 모노스페이스(Mono) 폰트 조합과 과감한 숫자/텍스트 스케일 대비 활용.
  - 무채색(블랙, 화이트, 다크그레이) 모노크롬을 기본으로 하고, 핵심 상태(드래그 선택, 여정, 오늘 등)에만 절제된 포인트 컬러(인터내셔널 레드/오렌지) 적용.
  - 여백(Whitespace)의 비효율적 낭비(불필요한 래핑, 과도한 패딩)를 줄이고, 한 줄 정렬(Compact 1-row layout) 및 일관된 그리드 정렬을 유지합니다.

