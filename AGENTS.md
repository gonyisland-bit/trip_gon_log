# 프로젝트 작업 지침 (AGENTS.md)

## 1. 언어 원칙
- 모든 설명, 보고, 계획서, 커뮤니케이션은 **한국어**로만 진행합니다.

## 2. 작업 프로세스 (원스톱 프로세스)
1. **계획 수립**: 일반 작업 착수 전 한국어로 상세 구현 계획서(Implementation Plan)를 아티팩트(Artifact) 파일로 작성하여 토글 공간에 제시 (`RequestFeedback: true`로 설정하여 Proceed 버튼 제공).
   - **Vercel 빌드/컴파일 에러 핫픽스 예외**: Vercel 배포 에러나 빌드/타입 에러 수정 요청 시에는 사전 계획서 승인 절차 없이 즉시 코드를 수정하고, 검증 후 Git push까지 원스톱으로 처리합니다.
2. **승인 대기**: 일반 기능 개발/수정 시에는 사용자 승인(Proceed) 전 임의 코드 수정 엄격 금지.
3. **구현 및 검증**: 승인 즉시 작업 수행 (주의: 터미널에서 `npm run build` 명령어는 제외하며 실행하지 않음. 단, Vercel 배포 에러 방지를 위해 Git 반영 직전 `npx tsc --noEmit` 타입 검사를 필수 실행하여 빌드 에러를 사전에 감지하고, 에러 발견 시 스스로 코드를 즉시 수정하여 무결점을 확인한 뒤 진행).
4. **Git 반영**: `npx tsc --noEmit` 무결점 검증 완료 즉시 `git add`, `commit`, `push`까지 일괄 완료.
5. **결과 보고 (Walkthrough)**: 작업 완료 즉시 구현 결과 및 변경 사항을 한국어로 정리한 Walkthrough 아티팩트(Artifact) 파일을 작성하여 토글 공간에 제시.

## 3. 디자인 원칙 (Swiss Minimal Design)
- **이모지 지양**: UI 라벨/버튼에 비규격 이모지 사용 금지, 정갈한 텍스트 및 Lucide 아이콘으로 통일.
- **기호 중복 배제**: 아이콘과 동일 의미 기호/텍스트 중복 표기 금지 (예: `<Plus /> ADD` 단일화).
- **군더더기 설명 배제**: 자명한 기능에 대한 장황한 보조 설명 제거, 여백과 타이포그래피 집중도 극대화.
- **스위스 국제 타이포그래피**: 무채색 모노크롬 기본 + 핵심 상태에만 절제된 포인트 컬러(레드) 적용. 단일 얇은 보더(`border border-black/20 dark:border-white/20`)와 폰트 위계 활용, 이중 컨테이너 및 두꺼운 배경 박스 배제.
- **폰트 굵기 규격화 (font-black 사용 금지)**: 지나치게 둔탁하고 시각적 피로감을 주는 `font-black` (font-weight: 900) 사용을 엄격히 금지하며, 헤드라인과 볼드 텍스트는 최대 `font-extrabold` (font-weight: 800) 이하로 단정하게 통일하여 정갈한 타이포그래피 위계 유지.
- **2단계 확인**: 중요 생성/삭제 동작은 스위스 미니멀 ConfirmModal을 통해 2단계 확인 필수.

## 4. 클라우드 서버 우선 영속성 원칙 (Cloud-First)
- **클라우드 서버 영속 저장**: 모든 신규 기능, 데이터, 설정, 보관함은 반드시 **Firebase Firestore** 및 **Cloudflare R2**에 영속 저장하여 다중 디바이스 간 완벽 동기화 보장.
- **localStorage 제한**: 즉각 렌더링(Optimistic UI) 및 오프라인 캐시 보조용으로만 제한 사용 (로컬 전용 저장 엄격 금지).
